import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.-gsMhh7K.js";const u=JSON.parse('{"title":"Instance methods","description":"","frontmatter":{},"headers":[],"relativePath":"reference/instance.md","filePath":"reference/instance.md"}'),d={name:"reference/instance.md"};function o(n,e,r,l,h,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="instance-methods" tabindex="-1">Instance methods <a class="header-anchor" href="#instance-methods" aria-label="Permalink to &quot;Instance methods&quot;">​</a></h1><p><code>createVibe()</code> returns a <code>VibeInstance</code>. Mount it once, retain it for the lifetime of its target, and destroy it during cleanup.</p><h2 id="lifecycle-and-state" tabindex="-1">Lifecycle and state <a class="header-anchor" href="#lifecycle-and-state" aria-label="Permalink to &quot;Lifecycle and state&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>mount()</code></td><td>Mounts the Vue tree and starts initial loading when needed.</td></tr><tr><td><code>destroy()</code></td><td>Unmounts Vibe and aborts its active page request.</td></tr><tr><td><code>getState()</code></td><td>Returns the current public state snapshot.</td></tr><tr><td><code>refresh()</code></td><td>Replaces the visible feed from its current continuation boundary.</td></tr><tr><td><code>reload()</code></td><td>Reloads the feed from its initial boundary.</td></tr><tr><td><code>loadNext()</code></td><td>Requests the next ordinary cursor page.</td></tr></tbody></table><p><code>refresh()</code> requests the current <code>next</code> cursor. When <code>next</code> is <code>null</code>, it falls back to the cursor that produced the last successfully loaded page so an exhausted feed can still be refreshed. <code>reload()</code> always requests the initial <code>null</code> cursor. Both methods replace the currently visible items and reset feed-owned fill, autofill, error, removal-history, and media-visibility state before loading. Each replacement therefore begins a new feed visit. Ordinary pagination and its retries remain in the current visit.</p><h2 id="item-removal-and-restoration" tabindex="-1">Item removal and restoration <a class="header-anchor" href="#item-removal-and-restoration" aria-label="Permalink to &quot;Item removal and restoration&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>removeItems(postIds, options?)</code></td><td>Animates loaded items out, removes them, records the transaction, and resolves with a <code>VibeRemoval</code>.</td></tr><tr><td><code>removeMediaAnimated(target)</code></td><td>Advances an active reel through Vibe&#39;s media/post transition, then removes the exact media and resolves with a <code>VibeMediaRemoval</code>.</td></tr><tr><td><code>removeMedia(target)</code></td><td>Removes exact media immediately and returns a <code>VibeMediaRemoval</code>.</td></tr><tr><td><code>restoreMediaRemoval(removal)</code></td><td>Restores an exact-media removal at its original group and media position.</td></tr><tr><td><code>restoreRemoval(removal)</code></td><td>Restores a removal transaction once, including one evicted from automatic undo history.</td></tr><tr><td><code>undoLastRemoval()</code></td><td>Restores and returns the latest recorded removal, or returns <code>null</code>.</td></tr><tr><td><code>restoreItems(placements)</code></td><td>Restores explicit item/index placements without creating an undo transaction.</td></tr><tr><td><code>updateItems(items)</code></td><td>Replaces loaded items that match <code>postId</code> and returns the identities that changed.</td></tr></tbody></table><p><code>VibeRemoval</code> is an opaque, readonly removal token whose entries contain the removed items and their original indexes. Pass the token back when application state, a failed request, or a specific queue action needs to restore that removal:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> removal</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vibe.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">removeItems</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;post-12&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;post-18&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Restores this transaction even if later removals happened.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">vibe.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">restoreRemoval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(removal)</span></span></code></pre></div><p>Multi-item removals retain Vibe&#39;s 35ms visible-card stagger by default. Pass <code>{ staggerMs: 0 }</code> when a coordinated batch should begin exiting in the same frame. The value must be a non-negative number.</p><p>For a conventional latest-action undo:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> restored</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vibe.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">undoLastRemoval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (restored) console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`Restored \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">restored</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">} items\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Vibe preserves the field&#39;s logical item order, so restoring removal transactions out of sequence still places their items correctly. Each instance retains the latest 20 removals by default. Configure <code>removalHistoryLimit</code> to use another non-negative limit; <code>0</code> disables <code>undoLastRemoval()</code> history without invalidating the tokens returned by <code>removeItems()</code>. Reloading or destroying the instance clears its history.</p><p><code>restoreItems()</code> remains available for consumer-created <code>{ item, index }</code> placements. Indexes must be non-negative integers. Items whose <code>postId</code> is already loaded are ignored. Passing a current <code>VibeRemoval</code> to this method is equivalent to <code>restoreRemoval()</code>.</p><p>All restored cards use the same entry motion as newly loaded cards.</p><p><code>updateItems(items)</code> replaces already-loaded posts in place. Matching uses <code>postId</code>, including when the stored identity is a number and the update uses the same digits as a string. Unknown identities are ignored and never inserted. The method does not load pages, change cursors, or play entry motion. It keeps the stored <code>postId</code> so mounted cards stay keyed, and it clamps that post&#39;s media index when the replacement has fewer assets.</p><p>With <code>removalReconciliation</code> enabled, each provider request records the unique items it contributed. Before <code>loadNext()</code>, Vibe compares the surviving contribution of the most recent pages with the configured provider page size. It proceeds directly when every page is full; otherwise it replays from the earliest underfilled page, appends all newly surfaced unique items, and then continues from the refreshed next cursor. A top-level removal is tombstoned until it is restored or the feed is reset. Restoring after reconciliation keeps both the restored item and any appended replacements.</p><p>Use <code>removeMediaAnimated()</code> for reactions or other actions against the media currently visible in a reel. Vibe completes the horizontal group transition or vertical post transition before committing the exact removal. When no loaded post follows, the reel remains open while its configured forward loader reaches the next page, an error, or the end of the feed. <code>removeMedia()</code> remains the immediate, backward-compatible primitive for consumers that own their own transition.</p><h2 id="layout-and-navigation" tabindex="-1">Layout and navigation <a class="header-anchor" href="#layout-and-navigation" aria-label="Permalink to &quot;Layout and navigation&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>setLayout(mode)</code></td><td>Changes between masonry, reel, and responsive modes.</td></tr><tr><td><code>navigateToReelItem(target)</code></td><td>Selects a loaded post and media asset by stable <code>postId</code> and <code>mediaId</code>.</td></tr><tr><td><code>nextReelPost()</code> / <code>previousReelPost()</code></td><td>Moves vertically between loaded posts.</td></tr><tr><td><code>nextReelMediaItem()</code> / <code>previousReelMediaItem()</code></td><td>Moves horizontally within grouped media.</td></tr><tr><td><code>setReelInfoSheet(enabled)</code></td><td>Opens or closes the configured information sheet.</td></tr><tr><td><code>setReelAutoAdvance(update)</code></td><td>Enables, disables, or updates reel auto advance.</td></tr></tbody></table><p>The next/previous navigation methods return <code>true</code> when a navigation was accepted and <code>false</code> when it was a no-op.</p><p><code>navigateToReelItem({ postId, mediaId })</code> returns <code>navigated</code> when both stable identities resolve in the active reel, including when that exact item is already active. It returns <code>not-found</code> when either identity is absent from the loaded feed, and <code>reel-inactive</code> before mount, after destroy, during initial loading, or while only masonry is visible. It never loads, inserts, or falls back to another item.</p><h2 id="loading-controls" tabindex="-1">Loading controls <a class="header-anchor" href="#loading-controls" aria-label="Permalink to &quot;Loading controls&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>setInfiniteScroll(enabled)</code></td><td>Changes automatic boundary loading.</td></tr><tr><td><code>setLoadMoreLocked(locked)</code></td><td>Pauses or resumes ordinary forward pagination.</td></tr><tr><td><code>fill(target)</code></td><td>Collects a requested number of pages or continues to the end.</td></tr><tr><td><code>cancelFill()</code></td><td>Cancels active manual fill work.</td></tr><tr><td><code>cancelAutofill()</code></td><td>Cancels active autofill work.</td></tr><tr><td><code>applyFillUpdate(update)</code></td><td>Applies a sequenced backend fill update.</td></tr><tr><td><code>applyAutofillUpdate(update)</code></td><td>Applies a sequenced backend autofill update.</td></tr><tr><td><code>restoreFillSession(snapshot)</code></td><td>Restores fill lifecycle state received after construction.</td></tr><tr><td><code>restoreAutofillSession(snapshot)</code></td><td>Restores autofill lifecycle state received after construction.</td></tr></tbody></table><h2 id="masonry-auto-scroll" tabindex="-1">Masonry auto scroll <a class="header-anchor" href="#masonry-auto-scroll" aria-label="Permalink to &quot;Masonry auto scroll&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>setAutoScroll(enabled, speed?)</code></td><td>Enables or disables auto scroll and optionally changes its speed.</td></tr><tr><td><code>setAutoScrollSpeed(speed)</code></td><td>Updates the requested speed within configured bounds.</td></tr><tr><td><code>pauseAutoScroll()</code></td><td>Pauses without disabling the feature.</td></tr><tr><td><code>resumeAutoScroll()</code></td><td>Resumes a paused controller.</td></tr></tbody></table><h2 id="example-cleanup-in-vue" tabindex="-1">Example cleanup in Vue <a class="header-anchor" href="#example-cleanup-in-vue" aria-label="Permalink to &quot;Example cleanup in Vue&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { onBeforeUnmount, onMounted } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;vue&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createVibe } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@wyxos/vibe&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> vibe</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createVibe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ target: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;#gallery&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, loadPage })</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onMounted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vibe.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onBeforeUnmount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vibe.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">destroy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())</span></span></code></pre></div>`,28)])])}const k=t(d,[["render",o]]);export{u as __pageData,k as default};
